import streamlit as st
import numpy as np
import matplotlib.pyplot as plt
import math

# Функция для поиска интервалов с корнями
def find_intervals(f, x_min, x_max, step=0.1):
    intervals = []
    x = x_min
    while x < x_max:
        try:
            f1 = f(x)
            f2 = f(x + step)
            if f1 * f2 < 0:
                intervals.append((x, x + step))
        except: pass
        x += step
    return intervals

# Функция для численного дифференцирования (для проверки сходимости)
def numerical_derivative(f, x, h=1e-5):
    try: return (f(x + h) - f(x - h)) / (2 * h)
    except: return None

# Собственная реализация метода простой итерации
def simple_iteration(g, x0, epsilon, max_iter):
    x_prev = x0
    iterations = [x0]
    errors = []
    
    for _ in range(max_iter):
        try:
            x_next = g(x_prev)
        except Exception as e:
            return None, None, f"Ошибка: {str(e)}"
        
        error = abs(x_next - x_prev)
        errors.append(error)
        iterations.append(x_next)
        
        if error < epsilon:
            break
            
        x_prev = x_next
    
    return iterations, errors, None

# Настройка страницы
st.set_page_config(page_title="Метод простой итерации", layout="wide")
st.title("🔍 Решение нелинейных уравнений (только метод простой итерации)")

# Боковая панель
with st.sidebar:
    st.header("Ввод уравнения")
    
    # Поле для f(x) = 0 в Python-формате
    f_str = st.text_input("f(x) = 0", value="x + math.cos(x) - 2")
    st.caption("Примеры: x + math.cos(x) - 2, x**2 - 2, math.exp(x) - x - 2")
    
    # Поле для g(x) = x
    g_str = st.text_input("Функция g(x) (x = g(x))", value="2 - math.cos(x)")
    st.caption("Примеры: 2 - math.cos(x), (x + 2/x)/2, math.log(x + 2)")
    
    x0 = st.number_input("Начальное приближение x0", 2.98, step=0.1)
    epsilon = st.number_input("Точность", 1e-6, format="%.6f")
    max_iter = st.slider("Макс. итераций", 10, 1000, 100)
    
    col1, col2 = st.columns(2)
    with col1: find_intervals_btn = st.button("Найти интервалы")
    with col2: study_convergence_btn = st.button("Исследовать скорость сходимости")
    
    st.info("Важно: Для метода простой итерации важно правильно преобразовать уравнение в вид x = g(x)")

# Кнопка "Рассчитать"
if st.sidebar.button("Рассчитать"):
    try:
        # Создаем функции f(x) и g(x)
        f = lambda x: eval(f_str, {"math": math, "np": np}, {"x": x})
        g = lambda x: eval(g_str, {"math": math, "np": np}, {"x": x})
        
        # Поиск интервалов
        intervals = find_intervals(f, x0-5, x0+5)
        
        # Выполняем метод простой итерации
        iterations, errors, error_msg = simple_iteration(g, x0, epsilon, max_iter)
        if error_msg: raise Exception(error_msg)
        
        # Проверка условия сходимости
        deriv = numerical_derivative(g, iterations[-1])
        condition = "✅ Сходится" if deriv is not None and abs(deriv) < 1 else "⚠️ Не сходится"
        
        # Вывод результатов
        st.subheader("✅ Результаты вычислений")
        col1, col2 = st.columns(2)
        with col1:
            st.metric("Корень", f"{iterations[-1]:.8f}")
            st.metric("Итераций", len(iterations)-1)
            st.metric("Точность", f"{abs(iterations[-1]-iterations[-2]):.2e}")
        with col2:
            st.metric("g(x)", f"{g(iterations[-1]):.8f}")
            st.metric("|x - g(x)|", f"{abs(iterations[-1]-g(iterations[-1])):.2e}")
            st.metric("Сходимость", condition)
        
        # Интервалы
        st.subheader("🔍 Интервалы с корнями")
        if intervals:
            for i, (a, b) in enumerate(intervals):
                st.write(f"Интервал {i+1}: [{a:.4f}, {b:.4f}]")
        else:
            st.warning("Интервалы не найдены")
        
        # График процесса сходимости
        st.subheader("📊 График процесса сходимости")
        fig, ax = plt.subplots(figsize=(10, 6))
        x_min, x_max = min(iterations)-1, max(iterations)+1
        x_vals = np.linspace(x_min, x_max, 1000)
        y_g = [g(x) for x in x_vals]
        y_x = x_vals
        
        ax.plot(x_vals, y_g, 'b-', label='y = g(x)')
        ax.plot(x_vals, y_x, 'r--', label='y = x')
        
        for i in range(len(iterations)-1):
            ax.plot([iterations[i], iterations[i+1]], [iterations[i], iterations[i]], 'g-', alpha=0.8)
            ax.plot([iterations[i+1], iterations[i+1]], [iterations[i], iterations[i+1]], 'g-', alpha=0.8)
        
        ax.scatter(iterations, iterations, color='red', s=50)
        ax.set_xlabel('x'); ax.set_ylabel('y'); ax.grid(True); ax.legend()
        st.pyplot(fig)
        
        # График скорости сходимости
        st.subheader("📈 График скорости сходимости")
        fig2, ax2 = plt.subplots(figsize=(10, 6))
        ax2.plot(range(len(errors)), np.log10(errors), 'bo-')
        ax2.set_xlabel('Номер итерации'); ax2.set_ylabel('log10(ошибка)'); ax2.grid(True)
        st.pyplot(fig2)
        
        # Исследование скорости от точности
        if study_convergence_btn:
            st.subheader("📊 Зависимость итераций от точности")
            epsilons = [10**(-i) for i in range(2, 11)]
            iters = []
            for eps in epsilons:
                result_iter = simple_iteration(g, x0, eps, max_iter)
                if result_iter[0] is not None:
                    iters.append(len(result_iter[0])-1)
            
            fig3, ax3 = plt.subplots(figsize=(10, 6))
            ax3.plot(np.log10(epsilons), iters, 'go-')
            ax3.set_xlabel('log10(точность)'); ax3.set_ylabel('Число итераций'); ax3.grid(True)
            st.pyplot(fig3)
    
    except Exception as e:
        st.error(f"❌ Ошибка: {str(e)}")
        st.markdown("""
        **Проверьте:**
        - Корректность функции g(x)
        - Начальное приближение
        - Ограничения функции (например, arccos от числа >1)
        """)
        st.code("""
        Примеры корректных функций:
        - Для x + cos(x) = 2: 2 - math.cos(x)
        - Для x^2 - 2 = 0: (x + 2/x)/2
        - Для x^3 - x - 1 = 0: (x + 1)**(1/3)
        """)

# Добавленная инструкция по использованию
st.markdown("""
---
## 📚 Инструкция по использованию программы

### 1. Подготовка уравнения
Для использования метода простой итерации необходимо:
- **Иметь уравнение вида f(x) = 0**
- **Преобразовать его в вид x = g(x)**

#### Как преобразовать уравнение в вид x = g(x):
1. **Для уравнения x + cos(x) = 2:**
   - Исходное уравнение: x + cos(x) = 2
   - Переносим cos(x) вправо: x = 2 - cos(x)
   - **f(x) = x + math.cos(x) - 2**
   - **g(x) = 2 - math.cos(x)**

2. **Для уравнения x² = 2:**
   - Исходное уравнение: x² = 2
   - Используем метод Ньютона для лучшей сходимости:
     - x = (x + 2/x)/2
   - **f(x) = x**2 - 2
   - **g(x) = (x + 2/x)/2**

3. **Для уравнения e^x = x + 2:**
   - Исходное уравнение: e^x = x + 2
   - Берем логарифм: x = ln(x + 2)
   - **f(x) = math.exp(x) - x - 2**
   - **g(x) = math.log(x + 2)**

### 2. Ввод в программу
1. **В поле "f(x) = 0"** введите функцию в формате Python:
   - Используйте `math.cos`, `math.sin`, `math.exp`, `math.log` для математических функций
   - Используйте `**` для возведения в степень
   - Пример: `x + math.cos(x) - 2`

2. **В поле "Функция g(x) (x = g(x))"** введите преобразованную функцию:
   - Пример для x + cos(x) = 2: `2 - math.cos(x)`
   - Пример для x² = 2: `(x + 2/x)/2`

3. **Задайте параметры:**
   - **Начальное приближение x0** - точка начала итераций
   - **Точность** - критерий остановки (обычно 1e-6)
   - **Макс. итераций** - предел для предотвращения зацикливания

### 3. Запуск и анализ результатов
1. **Нажмите "Рассчитать"** для выполнения метода
2. **Проанализируйте результаты:**
   - Найденный корень
   - Число итераций
   - Точность вычислений
   - Сходимость метода
3. **Используйте дополнительные кнопки:**
   - **"Найти интервалы"** - для определения отрезков, содержащих корни
   - **"Исследовать скорость сходимости"** - для анализа зависимости от точности

### 4. Проверка сходимости
Программа автоматически проверяет условие сходимости |g'(x)| < 1:
- ✅ **Сходится** - метод работает корректно
- ⚠️ **Не сходится** - попробуйте другое преобразование уравнения

### 5. Примеры для проверки
#### Пример 1: x + cos(x) = 2
- **f(x) = 0**: `x + math.cos(x) - 2`
- **g(x)**: `2 - math.cos(x)`
- **x0**: `2.98`

#### Пример 2: x² - 2 = 0
- **f(x) = 0**: `x**2 - 2`
- **g(x)**: `(x + 2/x)/2`
- **x0**: `1.5`

#### Пример 3: x³ - x - 1 = 0
- **f(x) = 0**: `x**3 - x - 1`
- **g(x)**: `(x + 1)**(1/3)`
- **x0**: `1.5`

### 6. Советы по преобразованию уравнений
1. **Проверяйте условие сходимости** |g'(x)| < 1
2. **Для полиномов** используйте метод Ньютона
3. **Для тригонометрических уравнений** выделяйте x явно
4. **Для экспоненциальных уравнений** используйте логарифмирование
5. **Если метод не сходится** - попробуйте другое преобразование

### 7. Ошибки и их решение
- **Ошибка "division by zero"** - измените начальное приближение
- **Ошибка "math domain error"** - проверьте область определения функции
- **Метод не сходится** - проверьте производную |g'(x)| < 1
- **Зацикливание** - уменьшите точность или увеличьте максимальное число итераций

> 💡 **Важно:** Метод простой итерации требует правильного преобразования уравнения. Если у вас возникают проблемы с поиском корня, скорее всего, нужно изменить форму g(x).
""")
